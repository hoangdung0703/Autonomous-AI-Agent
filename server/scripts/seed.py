# TODO: implement in a later step
# Purpose: Run once to ingest knowledge-base/ into Qdrant — scans
# knowledge-base/ recursively, detects category from folder name, runs the
# ingestion pipeline per file, logs per-file success/failure, and prints a
# summary (documents indexed, total chunks).
