# TODO: implement in a later step
# Purpose: calculate_or_verify(expression, context) tool.
# Safely evaluates a numeric expression using asteval (NOT Python eval()).
# Returns { "expression": ..., "result": ..., "context": ... }. This is the
# key differentiator from plain RAG: the agent computes, not just retrieves.
