# TODO: implement in a later step
# Purpose: list_documents(category=None) tool.
# Returns unique document names + category + chunk count from Qdrant
# payload aggregation, optionally filtered by category. Used for agent
# orientation before searching.
