# TODO: implement in a later step
# Purpose: search_knowledge_base(query, category=None) tool.
# Embeds the query via gemini-embedding-001, queries Qdrant with an optional
# category payload filter (hr | legal | ops), and returns the top 5 chunks
# (document_name, section_title, excerpt, similarity_score).
