# TODO: implement in a later step
# Purpose: compare_sections(doc1, doc2, topic) tool.
# Runs search_knowledge_base(topic) filtered to doc1 and doc2 (top 3 chunks
# each) and returns both result sets side by side for comparison.
