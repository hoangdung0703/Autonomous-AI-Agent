# TODO: implement in a later step
# Purpose: Text extraction from source documents.
# PDF -> pdfplumber first (fallback to OCR if extracted text < 100 chars);
# DOCX -> python-docx paragraph extraction; XLSX -> openpyxl, each sheet
# converted to a CSV-like text block.
