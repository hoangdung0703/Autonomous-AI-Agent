# TODO: implement in a later step
# Purpose: Semantic chunking strategy — splits at natural section boundaries
# (not fixed character count) using two pattern sets tried in order (generic
# headings, then VN legal-style headings), with paragraph-based fallback,
# oversized-chunk splitting, and undersized-chunk merging.
